name: build-windows
on:
  workflow_dispatch:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test-and-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Verify
        shell: pwsh
        run: python verify.py
      - name: Package
        shell: pwsh
        run: |
          pip install "pyinstaller>=6.11,<7"
          pyinstaller --noconfirm --clean --windowed --onefile --name OpenAITunnelManager --paths src launcher.py
      - uses: actions/upload-artifact@v4
        with:
          name: OpenAITunnelManager-windows
          path: dist/OpenAITunnelManager.exe
