<Window
    x:Class="OpenAITunnelManager.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:OpenAITunnelManager.App.Controls"
    mc:Ignorable="d"
    Title="OpenAI MCP Tunnel Manager">

    <Grid x:Name="RootGrid" Loaded="RootGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Padding="16,0,0,0" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <Image Source="Assets/AppIcon.png" Width="22" Height="22" />
                <TextBlock Text="OpenAI MCP Tunnel Manager" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <NavigationView
            x:Name="Navigation"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
            PaneDisplayMode="Auto"
            OpenPaneLength="216"
            CompactModeThresholdWidth="760"
            ExpandedModeThresholdWidth="1000"
            SelectionChanged="Navigation_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="概览" Tag="dashboard">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE80F;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="连接" Tag="connections" IsSelected="True">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE71B;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Content>
                <Grid x:Name="ContentGrid" Padding="24,20,24,16">
                    <Grid x:Name="DashboardPage" Visibility="Collapsed">
                        <ScrollViewer x:Name="DashboardScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalContentAlignment="Stretch">
                            <StackPanel x:Name="DashboardContentPanel" Spacing="20" HorizontalAlignment="Stretch">
                                <Grid x:Name="DashboardHeader">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel x:Name="DashboardHeaderTitle" Spacing="4">
                                        <TextBlock Text="概览" Style="{StaticResource TitleTextBlockStyle}" />
                                        <TextBlock Text="查看 tunnel-client 与本机 Tunnel 运行概况" Opacity="0.7" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <StackPanel x:Name="DashboardHeaderActions" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <ProgressRing Width="20" Height="20" IsActive="{Binding IsBusy}" />
                                        <Button Command="{Binding RefreshCommand}">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <FontIcon Glyph="&#xE72C;" FontSize="14" />
                                                <TextBlock Text="刷新" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Grid>

                                <Border Padding="4,2" Background="Transparent">
                                    <Grid ColumnSpacing="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon Glyph="&#xE930;" FontSize="14" VerticalAlignment="Top" Margin="0,3,0,0" />
                                        <StackPanel Grid.Column="1" Spacing="2">
                                            <TextBlock x:Name="DashboardSystemStatusText" Text="正在初始化" FontSize="16" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding StatusMessage}" Opacity="0.72" TextWrapping="Wrap" />
                                            <TextBlock x:Name="DashboardLastUpdatedText" Text="最后更新 --:--:--" FontSize="12" Opacity="0.55" />
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <TextBlock Text="运行概况" FontSize="18" FontWeight="SemiBold" Margin="0,4,0,-4" />
                                <Grid x:Name="DashboardCardsGrid" ColumnSpacing="12" RowSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Padding="18" MinHeight="112" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                        <StackPanel Spacing="7">
                                            <TextBlock Text="正在运行" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardRunningCount" Text="0" FontSize="32" FontWeight="SemiBold" />
                                            <TextBlock Text="当前活动实例" FontSize="12" Opacity="0.65" />
                                        </StackPanel>
                                    </Border>
                                    <Border Grid.Column="1" Padding="18" MinHeight="112" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                        <StackPanel Spacing="7">
                                            <TextBlock Text="Ready" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardReadyCount" Text="0" FontSize="32" FontWeight="SemiBold" />
                                            <TextBlock Text="健康并可用" FontSize="12" Opacity="0.65" />
                                        </StackPanel>
                                    </Border>
                                    <Border Grid.Column="2" Padding="18" MinHeight="112" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                        <StackPanel Spacing="7">
                                            <TextBlock Text="Profile" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardProfileCount" Text="0" FontSize="32" FontWeight="SemiBold" />
                                            <TextBlock Text="本机配置" FontSize="12" Opacity="0.65" />
                                        </StackPanel>
                                    </Border>
                                    <Border Grid.Column="3" Padding="18" MinHeight="112" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                        <StackPanel Spacing="7">
                                            <TextBlock Text="Runtime" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardRuntimeCount" Text="0" FontSize="32" FontWeight="SemiBold" />
                                            <TextBlock Text="Runtime Alias" FontSize="12" Opacity="0.65" />
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <TextBlock Text="tunnel-client" FontSize="18" FontWeight="SemiBold" Margin="0,4,0,-4" />
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                    <StackPanel>
                                        <Grid Padding="18,14" ColumnSpacing="20">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="2*" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="状态" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardClientStatusText" Grid.Column="1" Text="检测中" HorizontalAlignment="Right" FontWeight="SemiBold" />
                                        </Grid>
                                        <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                                        <Grid Padding="18,14" ColumnSpacing="20">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="2*" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="版本" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardClientVersionText" Grid.Column="1" Text="未检测" HorizontalAlignment="Right" TextTrimming="CharacterEllipsis" MaxLines="1" />
                                        </Grid>
                                        <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                                        <Grid Padding="18,14" ColumnSpacing="20">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="2*" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="可执行文件" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardClientPathText" Grid.Column="1" Text="未配置" HorizontalAlignment="Right" TextTrimming="CharacterEllipsis" MaxLines="1" />
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <TextBlock Text="Manager" FontSize="18" FontWeight="SemiBold" Margin="0,4,0,-4" />
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,8">
                                    <StackPanel>
                                        <Grid Padding="18,14" ColumnSpacing="20">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="2*" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="Windows 登录启动" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardStartupText" Grid.Column="1" Text="已关闭" HorizontalAlignment="Right" />
                                        </Grid>
                                        <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                                        <Grid Padding="18,14" ColumnSpacing="20">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="2*" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="关闭到系统托盘" Opacity="0.65" />
                                            <TextBlock x:Name="DashboardTrayText" Grid.Column="1" Text="已开启" HorizontalAlignment="Right" />
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="ConnectionsPage">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid x:Name="ConnectionsHeader">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel x:Name="ConnectionsHeaderTitle" Spacing="4">
                                <TextBlock Text="连接" Style="{StaticResource TitleTextBlockStyle}" />
                                <TextBlock Text="管理 Profile / Runtime，并在当前连接内查看日志和诊断" Opacity="0.7" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel x:Name="ConnectionsHeaderActions" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <ProgressRing Width="20" Height="20" IsActive="{Binding IsBusy}" />
                                <Button Click="AddProfile_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                                        <TextBlock Text="新建 Profile" />
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding RefreshCommand}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                                        <TextBlock Text="刷新" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <Grid x:Name="ConnectionsSplitGrid" Grid.Row="2" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30*" />
                                <ColumnDefinition Width="70*" />
                            </Grid.ColumnDefinitions>

                            <Border x:Name="ConnectionsListPanel" Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="配置列表" FontSize="16" FontWeight="SemiBold" />
                                    <ListView x:Name="ConnectionsList" Grid.Row="2" ItemsSource="{Binding Connections}" SelectedItem="{Binding SelectedConnection, Mode=TwoWay}" SelectionMode="Single" SelectionChanged="ConnectionsList_SelectionChanged">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Padding="8,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Spacing="3">
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                        <TextBlock Text="{Binding Subtitle}" FontSize="12" Opacity="0.65" TextTrimming="CharacterEllipsis" />
                                                    </StackPanel>
                                                    <Border Grid.Column="1" Padding="8,3" CornerRadius="8" VerticalAlignment="Center" Background="{ThemeResource ControlFillColorSecondaryBrush}">
                                                        <TextBlock Text="{Binding StatusLabel}" FontSize="12" />
                                                    </Border>
                                                </Grid>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </Border>

                            <Border x:Name="ConnectionsDetailsPanel" Grid.Column="1" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="8" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Spacing="4">
                                            <TextBlock Text="{Binding SelectedConnection.Name, FallbackValue=请选择配置}" FontSize="24" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding SelectedConnection.TunnelId}" Opacity="0.65" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <Border Grid.Column="1" Padding="10,5" CornerRadius="8" Background="{ThemeResource ControlFillColorSecondaryBrush}" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding SelectedConnection.StateText}" />
                                        </Border>
                                    </Grid>

                                    <StackPanel x:Name="ConnectionsPrimaryActions" Grid.Row="2" Orientation="Horizontal" Spacing="8">
                                        <Button Content="启动" Command="{Binding StartSelectedCommand}" IsEnabled="{Binding CanStartSelected}" />
                                        <Button Content="停止" Command="{Binding StopSelectedCommand}" IsEnabled="{Binding CanStopSelected}" />
                                        <Button Content="重启" Command="{Binding RestartSelectedCommand}" IsEnabled="{Binding CanRestartSelected}" />
                                        <Button Content="编辑 Profile" IsEnabled="{Binding CanEditSelected}" Click="EditProfile_Click" />
                                        <Button Content="更多" IsEnabled="{Binding CanDeleteSelected}">
                                            <Button.Flyout>
                                                <MenuFlyout>
                                                    <MenuFlyoutItem Text="本机偏好 / 密钥" Click="EditPreference_Click" />
                                                    <MenuFlyoutItem Text="打开配置位置" IsEnabled="{Binding CanOpenConfigSelected}" Click="OpenConfigLocation_Click" />
                                                    <MenuFlyoutSeparator />
                                                    <MenuFlyoutItem Text="删除" Click="DeleteSelected_Click" />
                                                </MenuFlyout>
                                            </Button.Flyout>
                                        </Button>
                                    </StackPanel>

                                    <TabView x:Name="ConnectionTabs" Grid.Row="4" IsAddTabButtonVisible="False" CanDragTabs="False" CanReorderTabs="False" SelectionChanged="ConnectionTabs_SelectionChanged">
                                        <TabViewItem Header="常规" IsClosable="False">
                                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                <StackPanel Spacing="18" Padding="2,8,2,4">
                                                    <TextBlock Text="基本信息" FontSize="16" FontWeight="SemiBold" />
                                                    <Grid x:Name="ConnectionInfoGrid" ColumnSpacing="20" RowSpacing="10">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="28*" />
                                                            <ColumnDefinition Width="72*" />
                                                        </Grid.ColumnDefinitions>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock Text="来源" Opacity="0.65" />
                                                        <TextBlock Grid.Column="1" Text="{Binding SelectedConnection.SourceText}" TextWrapping="Wrap" />
                                                        <TextBlock Grid.Row="1" Text="Profile" Opacity="0.65" />
                                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedConnection.ProfileName}" TextWrapping="Wrap" />
                                                        <TextBlock Grid.Row="2" Text="Profile 路径" Opacity="0.65" />
                                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedConnection.ProfilePath}" TextWrapping="Wrap" />
                                                        <TextBlock Grid.Row="3" Text="Runtime Alias" Opacity="0.65" />
                                                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedConnection.RuntimeAlias}" TextWrapping="Wrap" />
                                                        <TextBlock Grid.Row="4" Text="MCP Target" Opacity="0.65" />
                                                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedConnection.TargetValue}" TextWrapping="Wrap" />
                                                        <TextBlock Grid.Row="5" Text="PID" Opacity="0.65" />
                                                        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedConnection.ProcessId}" />
                                                        <TextBlock Grid.Row="6" Text="日志文件" Opacity="0.65" />
                                                        <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding CurrentLogPath}" TextWrapping="Wrap" />
                                                    </Grid>

                                                    <TextBlock Text="健康状态" FontSize="16" FontWeight="SemiBold" Margin="0,6,0,0" />
                                                    <Grid x:Name="ConnectionHealthGrid" ColumnSpacing="20" RowSpacing="10">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="28*" />
                                                            <ColumnDefinition Width="72*" />
                                                        </Grid.ColumnDefinitions>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock Text="状态" Opacity="0.65" />
                                                        <TextBlock Grid.Column="1" Text="{Binding SelectedConnection.StateText}" />
                                                        <TextBlock Grid.Row="1" Text="Health" Opacity="0.65" />
                                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedConnection.HealthText}" />
                                                        <TextBlock Grid.Row="2" Text="Ready" Opacity="0.65" />
                                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedConnection.ReadyText}" />
                                                        <TextBlock Grid.Row="3" Text="Health URL" Opacity="0.65" />
                                                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedConnection.HealthUrl}" TextWrapping="Wrap" />
                                                    </Grid>
                                                </StackPanel>
                                            </ScrollViewer>
                                        </TabViewItem>

                                        <TabViewItem Header="日志" IsClosable="False">
                                            <Grid Padding="2,8,2,4">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="10" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="10" />
                                                    <RowDefinition Height="*" />
                                                </Grid.RowDefinitions>
                                                <Grid x:Name="LogsHeader">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel x:Name="LogsHeaderTitle" Spacing="3">
                                                        <TextBlock Text="运行日志" FontSize="18" FontWeight="SemiBold" />
                                                        <TextBlock Text="读取当前连接的 Runtime 或 GUI 前台 Profile 日志" Opacity="0.7" TextWrapping="Wrap" />
                                                    </StackPanel>
                                                    <StackPanel x:Name="LogsHeaderActions" Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="8">
                                                        <Button Content="立即刷新" Click="AnsiLogManualRefresh_Click" />
                                                        <Button Content="打开文件位置" IsEnabled="{Binding CanOpenLogSelected}" Click="OpenLogLocation_Click" />
                                                    </StackPanel>
                                                </Grid>

                                                <Grid x:Name="LogsFilterGrid" Grid.Row="2" ColumnSpacing="10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="42*" />
                                                        <ColumnDefinition Width="22*" />
                                                        <ColumnDefinition Width="18*" />
                                                        <ColumnDefinition Width="18*" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBox PlaceholderText="搜索日志" Text="{Binding LogSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                    <ComboBox Grid.Column="1" x:Name="LogLevelCombo" SelectedValue="{Binding LogLevel, Mode=TwoWay}" SelectedValuePath="Content">
                                                        <ComboBoxItem Content="全部" IsSelected="True" />
                                                        <ComboBoxItem Content="DEBUG" />
                                                        <ComboBoxItem Content="INFO" />
                                                        <ComboBoxItem Content="WARN" />
                                                        <ComboBoxItem Content="ERROR" />
                                                    </ComboBox>
                                                    <CheckBox Grid.Column="2" Content="自动刷新" IsChecked="{Binding LogAutoRefresh, Mode=TwoWay}" VerticalAlignment="Center" />
                                                    <CheckBox Grid.Column="3" x:Name="LogWrapCheckBox" Content="自动换行" IsChecked="{Binding LogWrap, Mode=TwoWay}" VerticalAlignment="Center" />
                                                </Grid>

                                                <Border Grid.Row="4" Background="{ThemeResource ControlFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                                                    <controls:AnsiLogViewerControl
                                                        x:Name="AnsiLogViewer"
                                                        HorizontalAlignment="Stretch"
                                                        VerticalAlignment="Stretch" />
                                                </Border>
                                            </Grid>
                                        </TabViewItem>

                                        <TabViewItem Header="诊断" IsClosable="False">
                                            <Grid Padding="2,8,2,4">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="12" />
                                                    <RowDefinition Height="*" />
                                                </Grid.RowDefinitions>
                                                <Grid x:Name="DiagnosticsHeader">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel x:Name="DiagnosticsHeaderTitle" Spacing="3">
                                                        <TextBlock Text="诊断" FontSize="18" FontWeight="SemiBold" />
                                                        <TextBlock Text="运行 doctor --explain 并检查当前连接的详细 Health" Opacity="0.7" TextWrapping="Wrap" />
                                                    </StackPanel>
                                                    <StackPanel x:Name="DiagnosticsHeaderActions" Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="8">
                                                        <Button Content="详细 Health" Command="{Binding RefreshHealthCommand}" />
                                                        <Button Content="开始诊断" Command="{Binding DiagnoseSelectedCommand}" IsEnabled="{Binding CanDiagnoseSelected}" />
                                                    </StackPanel>
                                                </Grid>

                                                <Grid x:Name="DiagnosticsBody" Grid.Row="2" ColumnSpacing="12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <Border x:Name="DiagnosticsDoctorPanel" Padding="14" Background="{ThemeResource ControlFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="10" />
                                                                <RowDefinition Height="*" />
                                                            </Grid.RowDefinitions>
                                                            <TextBlock Text="Doctor 输出" FontSize="16" FontWeight="SemiBold" />
                                                            <TextBox Grid.Row="2" Text="{Binding DiagnosticText}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" BorderThickness="0" Background="Transparent" />
                                                        </Grid>
                                                    </Border>
                                                    <Border x:Name="DiagnosticsHealthPanel" Grid.Column="1" Padding="14" Background="{ThemeResource ControlFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="10" />
                                                                <RowDefinition Height="*" />
                                                            </Grid.RowDefinitions>
                                                            <TextBlock Text="Health 输出" FontSize="16" FontWeight="SemiBold" />
                                                            <TextBox Grid.Row="2" Text="{Binding HealthText}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" BorderThickness="0" Background="Transparent" />
                                                        </Grid>
                                                    </Border>
                                                </Grid>
                                            </Grid>
                                        </TabViewItem>
                                    </TabView>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>

                    <Grid x:Name="SettingsPage" Visibility="Collapsed">
                        <ScrollViewer x:Name="SettingsScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalContentAlignment="Stretch">
                            <StackPanel x:Name="SettingsContentPanel" Spacing="18" HorizontalAlignment="Stretch">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="设置" Style="{StaticResource TitleTextBlockStyle}" />
                                    <TextBlock Text="Manager 本机设置；修改后立即应用" Opacity="0.7" TextWrapping="Wrap" />
                                </StackPanel>

                                <InfoBar x:Name="MissingClientInfo" IsOpen="True" IsClosable="False" Severity="Warning" Title="tunnel-client" Message="如果未检测到 tunnel-client，请下载官方最新版或选择自定义 tunnel-client.exe。" />

                                <TextBlock Text="常规设置" FontSize="16" FontWeight="SemiBold" Margin="0,2,0,-4" />
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                                    <StackPanel>
                                        <Grid x:Name="SettingsClientGrid" Padding="18" ColumnSpacing="16">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="28*" />
                                                <ColumnDefinition Width="72*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Spacing="3">
                                                <TextBlock Text="tunnel-client" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding TunnelClientModeText}" Opacity="0.65" FontSize="12" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Spacing="5">
                                                <TextBlock Text="{Binding ManagedTunnelClientVersionText}" />
                                                <TextBlock Text="{Binding ManagedTunnelClientPath}" Opacity="0.65" FontSize="12" TextTrimming="CharacterEllipsis" />
                                                <TextBox Text="{Binding TunnelClientPath}" IsReadOnly="True" PlaceholderText="自定义 tunnel-client.exe 路径（可选）" />
                                                <TextBlock Text="{Binding TunnelClientUpdateStatus}" Opacity="0.65" FontSize="12" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" Spacing="6" VerticalAlignment="Center">
                                                <ProgressRing Width="18" Height="18" IsActive="{Binding IsTunnelClientUpdating}" />
                                                <Button Content="下载/更新" Click="DownloadManagedTunnelClient_Click" />
                                                <Button Content="浏览自定义" Click="BrowseTunnelClient_Click" />
                                            </StackPanel>
                                        </Grid>
                                        <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                                        <Grid Padding="18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="外观" FontWeight="SemiBold" />
                                                <TextBlock Text="选择 Manager 的界面主题" Opacity="0.65" FontSize="12" />
                                            </StackPanel>
                                            <ComboBox
                                                x:Name="ThemeModeCombo"
                                                Grid.Column="1"
                                                MinWidth="150"
                                                HorizontalAlignment="Right"
                                                SelectedValuePath="Tag"
                                                SelectionChanged="ThemeModeCombo_SelectionChanged">
                                                <ComboBoxItem Content="跟随系统" Tag="system" />
                                                <ComboBoxItem Content="浅色" Tag="light" />
                                                <ComboBoxItem Content="深色" Tag="dark" />
                                            </ComboBox>
                                        </Grid>
                                        <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                                        <Grid Padding="18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="关闭到系统托盘" FontWeight="SemiBold" />
                                                <TextBlock Text="关闭主窗口后，Manager 继续在后台运行" Opacity="0.65" FontSize="12" />
                                            </StackPanel>
                                            <ToggleSwitch Grid.Column="1" Tag="closeToTray" IsOn="{Binding CloseToTray, Mode=TwoWay}" Toggled="ImmediateSettingToggle_Toggled" />
                                        </Grid>
                                        <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                                        <Grid Padding="18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="Windows 登录时启动" FontWeight="SemiBold" />
                                                <TextBlock Text="使用当前 EXE 注册用户级启动项" Opacity="0.65" FontSize="12" />
                                            </StackPanel>
                                            <ToggleSwitch Grid.Column="1" Tag="startWithWindows" IsOn="{Binding StartWithWindows, Mode=TwoWay}" Toggled="ImmediateSettingToggle_Toggled" />
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <TextBlock Text="高级设置" FontSize="16" FontWeight="SemiBold" Margin="0,2,0,-4" />
                                <Border Padding="18" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                                    <StackPanel Spacing="16">
                                        <TextBlock Text="可选：为 tunnel-client 子进程覆盖 Profile / State 目录。留空时继承 tunnel-client 当前环境或默认目录。" TextWrapping="Wrap" Opacity="0.7" />

                                        <StackPanel Spacing="6">
                                            <TextBlock Text="Profile 目录覆盖" FontWeight="SemiBold" />
                                            <TextBlock Text="TUNNEL_CLIENT_PROFILE_DIR" FontSize="12" Opacity="0.65" />
                                            <Grid ColumnSpacing="8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox Text="{Binding ProfileDirectoryOverride, Mode=TwoWay}" IsReadOnly="True" PlaceholderText="未覆盖" MinWidth="0" />
                                                <Button Grid.Column="1" Content="选择目录" Tag="profile" Click="BrowseDirectoryOverride_Click" />
                                                <Button Grid.Column="2" Content="清除" Tag="profile" Click="ClearDirectoryOverride_Click" />
                                            </Grid>
                                        </StackPanel>

                                        <StackPanel Spacing="6">
                                            <TextBlock Text="State 目录覆盖" FontWeight="SemiBold" />
                                            <TextBlock Text="TUNNEL_CLIENT_STATE_DIR" FontSize="12" Opacity="0.65" />
                                            <Grid ColumnSpacing="8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox Text="{Binding StateDirectoryOverride, Mode=TwoWay}" IsReadOnly="True" PlaceholderText="未覆盖" MinWidth="0" />
                                                <Button Grid.Column="1" Content="选择目录" Tag="state" Click="BrowseDirectoryOverride_Click" />
                                                <Button Grid.Column="2" Content="清除" Tag="state" Click="ClearDirectoryOverride_Click" />
                                            </Grid>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Window>